
import React from 'react';
import {
    SafeAreaView,
    Text,
} from 'react-native';

const Header = () => {
     
    return (
        <SafeAreaView style={{ flex: 1, marginRight: 30 }}>
            <Text>TVMAZE</Text>
        </SafeAreaView>
    );
}

export default Header;
